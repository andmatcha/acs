# `app` モジュール

## 責務

- アプリケーション層の入口です。
- 現状は `cli` サブモジュールを公開し、実行開始点をまとめています。

## 主な処理

- `src/main.rs` から呼ばれる `app::cli::run()` を提供します。
- 今後 CLI 以外の起動形態を増やす場合の受け皿になれる構成です。

## 実装の要点

- `mod.rs` は薄く保たれており、実処理は `app/cli` 側へ委譲しています。
- 「入口だけを持つモジュール」にすることで、起動経路の責務を明確にしています。
